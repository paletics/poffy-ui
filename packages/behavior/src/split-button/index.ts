/**
 * Owns an uncontrolled split-button menu's disclosure, active-item index, and menu keyboard flow.
 *
 * Render the primary action and restore focus outside this hook.
 */
export { useSplitButton } from './useSplitButton';

/** Split-button menu item, hook configuration, and return contracts. */
export type {
  SplitButtonBehaviorItem,
  UseSplitButtonOptions,
  UseSplitButtonReturn,
} from './useSplitButton.types';
