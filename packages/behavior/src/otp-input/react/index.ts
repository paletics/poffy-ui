/** Owns or coordinates normalized OTP segments, composition, and focus movement for React. */
export { useOtpInputState } from '../useOtpInputState';

/** React OTP state-hook configuration, keyboard, and return contracts. */
export type {
  OtpInputKeyboardInput,
  OtpInputKeyboardResult,
  UseOtpInputStateOptions,
  UseOtpInputStateReturn,
} from '../useOtpInputState.types';
