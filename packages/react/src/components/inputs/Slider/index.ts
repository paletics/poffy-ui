/** Renders a styled native range input with FormControl-aware accessibility and read-only guarding. */
export * from './Slider';

/** Slider visual variants and native range-input props. */
export type * from './Slider.types';
