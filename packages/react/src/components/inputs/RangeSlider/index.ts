/**
 * Renders an accessible two-thumb numeric interval slider with optional hidden form fields.
 *
 * Values are normalized against bounds, step, and minimum thumb separation.
 */
export * from './RangeSlider';

/** RangeSlider values, thumb identities, labels, and controlled/uncontrolled props. */
export type * from './RangeSlider.types';
