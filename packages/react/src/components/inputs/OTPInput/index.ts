/**
 * Renders a fixed-length, accessible OTP field with digit normalization, IME/paste support, and
 * optional joined-value form submission.
 */
export * from './OTPInput';

/** OTPInput values, locale messages, visual variants, and controlled/uncontrolled props. */
export type * from './OTPInput.types';
