/**
 * Renders a keyboard-visible-on-focus fragment link for bypassing repeated page chrome.
 *
 * The host owns the matching target and any programmatic focus transfer.
 */
export { SkipLink } from './SkipLink';

/** SkipLink fragment destination and anchor-prop contract. */
export type { SkipLinkProps } from './SkipLink.types';
