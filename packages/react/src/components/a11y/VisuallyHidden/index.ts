/**
 * Visually hides content while retaining it for assistive technology and reading order.
 *
 * Focusable descendants become visible while focus remains within the wrapper.
 */
export * from './VisuallyHidden';

/** VisuallyHidden default-host, asChild-host, and component contracts. */
export type * from './VisuallyHidden.types';
