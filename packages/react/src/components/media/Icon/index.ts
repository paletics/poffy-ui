/** Base SVG primitive; decorative by default and accessible when explicitly named. */
export * from './Icon';
export type * from './Icon.types';
/** Built-in named SVG icon components, each accepting `IconProps` except `variant`. */
export * from './icons';
